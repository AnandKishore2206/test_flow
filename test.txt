<div class="main-details-container">
  <div class="base-details">
    <!-- Base Details Content -->
  </div>

  <div class="right-section">
    <div class="dates">
      <!-- Dates Content -->
    </div>
    <div class="contacts">
      <!-- Contacts Content -->
    </div>
  </div>
</div>


.main-details-container {
  display: grid;
  grid-template-columns: 2fr 1fr; /* Left is twice as big */
  gap: 1rem;
  height: 100%; /* Fill available space */
}

.base-details {
  background: #f9f9f9;
  padding: 1rem;
  border-radius: 8px;
  overflow-y: auto;
}

.right-section {
  display: flex;
  flex-direction: column;
  gap: 1rem;
  height: 100%;
}

.dates, .contacts {
  background: #f9f9f9;
  padding: 1rem;
  border-radius: 8px;
  flex: 1 1 auto; /* Grow nicely */
  overflow-y: auto;
}

/* Responsive behavior */
@media (max-width: 1024px) {
  .main-details-container {
    grid-template-columns: 1fr; /* Stack vertically on tablets and below */
  }

  .right-section {
    flex-direction: row; /* Dates and Contacts side-by-side on tablet */
    gap: 1rem;
  }

  .dates, .contacts {
    flex: 1 1 50%; /* Each take half width */
  }
}

@media (max-width: 600px) {
  .right-section {
    flex-direction: column; /* Stack Dates and Contacts vertically on small mobiles */
  }
}
