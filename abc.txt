<div class="description-box" [class.expanded]="showFull">
  <div class="content">
    {{ text }}
  </div>
</div>

<div class="toggle" (click)="toggle()">
  {{ showFull ? 'Show less' : 'Read more' }}
</div>

.description-box {
  max-height: 150px;
  overflow: hidden;
  border: 1px solid #ccc;
  padding: 1rem;
  border-radius: 6px;
  background-color: #fafafa;
  transition: max-height 0.3s ease;

  &.expanded {
    max-height: 300px; // or whatever max height you prefer
    overflow-y: auto;
  }

  .content {
    white-space: pre-line;
    line-height: 1.5;
  }
}

.toggle {
  margin-top: 0.5rem;
  color: #007ad9;
  cursor: pointer;
  font-size: 0.85rem;
  user-select: none;
}


import { Component, Input } from '@angular/core';

@Component({
  selector: 'app-see-more-text',
  templateUrl: './see-more-text.component.html',
  styleUrls: ['./see-more-text.component.scss']
})
export class SeeMoreTextComponent {
  @Input() text = '';
  showFull = false;

  toggle() {
    this.showFull = !this.showFull;
  }
}
