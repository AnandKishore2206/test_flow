<div class="box base-details">
  <ng-container *ngIf="isLoading; else baseContent">
    <div class="skeleton-line" *ngFor="let line of skeletonLines"></div>
  </ng-container>

  <ng-template #baseContent>
    <!-- Actual Base Details Content -->
  </ng-template>
</div>

isLoading = true;
skeletonLines = Array(4); // number of lines you'd like to show initially

ngOnInit() {
  // Simulate load or subscribe to actual data
  setTimeout(() => {
    this.isLoading = false;
  }, 2000);
}

.skeleton-line {
  height: 1rem;
  background: linear-gradient(90deg, #eee, #f5f5f5, #eee);
  animation: skeleton-loading 1.5s infinite linear;
  border-radius: 4px;
  margin-bottom: 0.5rem;

  &:last-child {
    margin-bottom: 0;
  }
}

@keyframes skeleton-loading {
  0% {
    background-position: -200px 0;
  }
  100% {
    background-position: calc(200px + 100%) 0;
  }
}
