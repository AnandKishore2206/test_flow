<div class="description-wrapper">
  <div class="description-box" [class.expanded]="showFull" #descBox>
    <div class="description-text" [innerHTML]="description"></div>
  </div>
  <button pButton class="p-button-text toggle-btn" (click)="toggleDescription()">
    {{ showFull ? 'Show less' : 'Read more' }}
  </button>
</div>


.description-wrapper {
  position: relative;
  max-width: 100%;
}

.description-box {
  max-height: 200px;
  overflow: hidden;
  transition: max-height 0.5s ease;
  padding-right: 1rem;
  position: relative;

  .description-text {
    white-space: pre-line;
    line-height: 1.5;
  }

  &.expanded {
    max-height: 500px; // Can be increased based on needs
    overflow-y: auto;
  }
}

.toggle-btn {
  margin-top: 0.5rem;
  font-size: 0.875rem;
  padding: 0.25rem 0.5rem;
}


showFull = false;

toggleDescription() {
  this.showFull = !this.showFull;
}
